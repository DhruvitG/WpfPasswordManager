<Window x:Class="WpfPasswordManager.AccountListWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WpfPasswordManager"
        mc:Ignorable="d"
        Title="AccountListWindow" Height="400" Width="600">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="25" />
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>
        <ListView Name="lvDataBinding">
            <ListView.ItemTemplate>
                <DataTemplate>
                    <WrapPanel>
                        <TextBlock Name="accountId" Text="{Binding id}" Width="30"/>
                        <TextBlock Margin="10,0,0,0" Name="accountTitle" Text="{Binding title}"/>
                        <Button Click="onEditBtnClicked">Edit</Button>
                        <Button Click="onDeleteBtnClicked">Delete</Button>
                    </WrapPanel>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
        <Button Click="onAddAccountBtnClicked" Grid.Row="1" Width="100" HorizontalAlignment="Right" Margin="0,0,-2,0">Add Account</Button>
    </Grid>
</Window>
